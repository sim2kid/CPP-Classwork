#include <stdio.h>
#include <stdlib.h>
#include <math.h>

/*
int * foo() {
	return (int *)malloc(sizeof(int));
}

void bar(int * i) {
	*i = 10;
	free(i);
}

int main() {
	//int *i = (int *)malloc(sizeof(int));
	int *z = foo();
	bar(z);
	printf("%d\0", *z);
	return 0;
}
*/

int main() {

}